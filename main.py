def readGraph(i, file):
    if i == 0:
        matrixGraph(file)
    else:
        listGraph(file)
def matrixGraph(file):
    myGraph = etc
def listGraph(file):
    myGraph = etc

def getInfo(graph):
    n
    m
    readGrau(graph)

def readGrau(graph)
    anotar todos os graus
    return min,max,méd e mediana

def BFS(vertice):

def DFS(vertice):

def createTreeBFS(vertice):

def createTreeDFS(vertice):

def dist(v1, v2):
    tree = createTreeBFS(v1)
    find(tree, v2)
def find(three, v):
    find v in tree
    return v.level

def getDiameter(graph):
    return tree.maxLevel